services:
    app:
        build:
            context: ..
            dockerfile: .devcontainer/Dockerfile
        volumes:
            - ..:/workspace:cached
            - C:\Users\carlo\.gemini:/home/researcher/.gemini
        command: sleep infinity
